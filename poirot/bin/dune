(executable
    (name poirot_inference)
    (modules poirot_inference)
    (libraries poirot))

(executable
    (name poirot_fuzzer)
    (modules poirot_fuzzer)
    (libraries poirot))

(executable
    (name poirot_quotient)
    (modules poirot_quotient)
    (libraries poirot))

(executable
    (name poirot_example)
    (modules poirot_example)
    (libraries poirot))

