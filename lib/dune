(ocamllex lexerbnf lexerconf)

(menhir
 (modules parserbnf))

(library
    (name poirot) (public_name poirot)
    (private_modules ("clean" grammar grammar_io inference lexerbnf lexerconf oracle parserbnf quotient fuzzer)))
